<nav class="navbar">
  <a href="#" class="logo">PetPawsHN</a>
  <ul class="nav-menu">
    {{#each navs}} 
      <li class="nav-item">
        <a href="{{url}}">{{label}}</a>
        {{#if subItems}} 
          <ul>
            {{#each subItems}}
              <li><a href="{{url}}">{{label}}</a></li>
            {{/each}}
          </ul>
        {{/if}}
      </li>
    {{/each}}
  </ul>
</nav>
